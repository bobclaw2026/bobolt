---
summary: "CLI reference for `bobolt uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `bobolt uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
bobolt uninstall
bobolt uninstall --all --yes
bobolt uninstall --dry-run
```
